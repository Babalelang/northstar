# web framework + server
fastapi==0.115.6
uvicorn[standard]==0.34.0

# database
sqlalchemy==2.0.36
psycopg2-binary==2.9.10

# reads DATABASE_URL and the sportsdb settings out of .venv/.env
python-dotenv==1.0.1

# request/response validation
pydantic==2.10.4

# used by services/sportsdb_service.py to call the free TheSportsDB api
requests==2.32.3
